# TEMPORARY COMPATIBILITY STUB — owned by Kavin (Member 2).
# Exists only so the API is runnable before the real recommender lands.
# Replace this whole file with the real implementation; keep the signature.


def recommend_movies(favorite_genres: list[str]) -> list[dict[str, str]]:
    """Temporary stub: returns no recommendations."""
    return []
